// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  // Your valid Next.js config options here
  // Remove any 'turbopack.loaders' if you're not using Turbopack
  reactStrictMode: true,
}

export default nextConfig;